<?php

/**
 * @file
 * Theming preprocesses
 */

/**
 * Preprocess for scs_newsletter.
 */
function template_preprocess_atwork_newsletter(&$vars, $hook) {
  dpm($vars);
  dpm($hook);
  // TODO: Convert $vars['comment'] and $vars['notes'] into desired arrays if they exist

}