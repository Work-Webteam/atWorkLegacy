/**
 * Implements hook_admin_menu_map().
 */
function reply_admin_menu_map() {
  if (!user_access('administer reply bundles')) {
    return;
  }

+  $map = array();

  $reply_bundles = reply_load_bundles();
  $path = 'admin/structure/reply/manage/%reply_bundle';
  $map[$path] = array(
    'parent' => 'admin/structure/reply',
    'arguments' => array(
      array('%reply_bundle' => array_keys($reply_bundles)),
    ),
  );


 - foreach ($reply_bundles as $bundle_name => $bundle_info) {
 -   $fields = array();
 -   foreach (field_info_instances('reply', $bundle_name) as $field) {
 -     $fields[] = $field['field_name'];
 -   }
 -   $arguments = array(
 -     '%reply_bundle' => array($bundle_name),
 -     '%field_ui_menu' => $fields,
 -   );
 -   if (!empty($arguments)) {
 -     $map["$path/fields/%field_ui_menu"]['parent'] = "$path/fields";
 -     $map["$path/fields/%field_ui_menu"]['arguments'][] = $arguments;
 -   }
 - }

  return $map;
}